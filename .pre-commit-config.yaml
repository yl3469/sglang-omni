default_stages: [pre-commit, pre-push, manual]

exclude: \.txt$

repos:
  - repo: https://github.com/PyCQA/autoflake
    rev: v2.3.1
    hooks:
    -   id: autoflake
        args: [--remove-all-unused-imports, --in-place]
        exclude: ^\.claude/skills/
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-symlinks
      - id: destroyed-symlinks
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        args: [--allow-multiple-documents]
      - id: check-toml
      - id: check-ast
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: check-shebang-scripts-are-executable
        exclude: (?:^\.claude/skills/|^sglang_omni_router/rust/tests/.*\.rs$)
      - id: detect-private-key
      - id: debug-statements
      - id: no-commit-to-branch
  - repo: https://github.com/PyCQA/isort
    rev: 5.13.2
    hooks:
      - id: isort
        exclude: ^\.claude/skills/
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.10
    hooks:
      - id: ruff
        args: [--select=F401, --fixable=F401]
        files: ^(benchmarks/|docs/|examples/)
        exclude: \.ipynb$
  - repo: https://github.com/psf/black
    rev: 24.10.0
    hooks:
      - id: black-jupyter
        exclude: ^\.claude/skills/
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v18.1.8
    hooks:
    - id: clang-format
      types_or: [c++, cuda]
      args: [--style=file, --verbose]
  - repo: https://github.com/kynan/nbstripout
    rev: 0.8.1
    hooks:
      - id: nbstripout
        args:
          - '--keep-output'
          - '--extra-keys=metadata.kernelspec metadata.language_info.version'
  - repo: local
    hooks:
      - id: sort-ci-permissions
        name: sort CI_PERMISSIONS.json
        entry: python3 .github/update_ci_permission.py --sort-only
        language: system
        files: ^\.github/CI_PERMISSIONS\.json$
        pass_filenames: false
      - id: rustfmt-sglang-omni-router
        name: rustfmt sglang-omni-router
        entry: bash -c 'cd sglang_omni_router/rust && cargo fmt --all -- --check'
        language: system
        files: ^sglang_omni_router/rust/.*\.rs$
        pass_filenames: false
